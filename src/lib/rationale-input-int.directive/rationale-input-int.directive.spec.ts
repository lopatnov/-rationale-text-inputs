import { RationaleInputIntDirective } from './rationale-input-int.directive';

describe('RationaleInputIntDirective', () => {
  it('should create an instance', () => {
    const directive = new RationaleInputIntDirective();
    expect(directive).toBeTruthy();
  });
});
