import { RationaleInputNumberDirective } from './rationale-input-number.directive';

describe('RationaleInputNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new RationaleInputNumberDirective();
    expect(directive).toBeTruthy();
  });
});
